model
{
  for(j in 1:ncounts){
    dif_true[j] <- difmod_y[year[j]]
    dif[j] ~ dt(dif_true[j],tau,nu)
  }


     difmod ~ dnorm(0,0.001)
    for(y in 1:nyears){
    difmod_yx[y] ~ dnorm(0,tauy)
    difmod_y[y] <- difmod + difmod_yx[y]
      }
  

	nu ~ dgamma(2, 0.1) #
  tau ~ dgamma(0.001,0.001)
  tauy ~ dgamma(0.001,0.001)



}#end model

