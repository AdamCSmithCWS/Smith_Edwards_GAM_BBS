model
{
  for(j in 1:ncounts){
    dif_true[j] <- difmod_s[strat[j]]
    dif[j] ~ dt(dif_true[j],tau,nu)
  }


     difmod ~ dnorm(0,0.001)
    for(y in 1:nstrat){
    difmod_sx[y] ~ dnorm(0,taus)
    difmod_s[y] <- difmod + difmod_sx[y]
      }
  

	nu ~ dgamma(2, 0.1) #
  tau ~ dgamma(0.001,0.001)
  tauy ~ dgamma(0.001,0.001)



}#end model

